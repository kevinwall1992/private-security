#include "Direction.h"

Direction::Enum Direction::GetDirection(Tile *source, Tile *destination)
{
	return Direction::Enum();
}

Tile * Direction::GetDestination(Tile *source, Direction::Enum)
{
	return nullptr;
}